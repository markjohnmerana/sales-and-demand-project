
fact_sales Row ID, Order ID, Product ID, Customer ID, Postal Code, Sales, Quantity, Discount, Profit
dim_order Order ID, Ship Date, Ship Mode
dim_customer Customer ID, Customer Name, Segment
dim_location Country, City, State, Postal Code, Region
dim_product Product ID, Category, Sub-Category, Product Name
dim_date Order Date, Day, Month, Quarter, Year

               dim_date
                  |
dim_customer — fact_sales — dim_product
                  |
             dim_location
                  |
               dim_order